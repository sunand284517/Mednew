global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Add your application metrics endpoint here
  # - job_name: 'medassist-backend'
  #   static_configs:
  #     - targets: ['host.docker.internal:8000']
